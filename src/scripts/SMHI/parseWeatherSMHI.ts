import * as weatherTypes from './weatherTypesSMHI'
import {WeatherData} from 'scripts/universalWeatherTypes'

export default function parseWeather(weatherData: weatherTypes.WeatherData): WeatherData {
        
}